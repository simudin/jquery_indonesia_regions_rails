require 'spec_helper'

describe JqueryIndonesiaRegionsRails do
  it 'has a version number' do
    expect(JqueryIndonesiaRegionsRails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
